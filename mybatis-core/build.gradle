dependencies {
    api project(':mybatis-annotation')
    api project(":mybatis-plugin")
    api project(':pageable')
    api project(':sequence')
    api "${libs.inject}"
    api "${libs.mybatis}"
    api "${libs.jsqlparser}"
    api "${libs.cglib}"
    api "${libs.caffeine}"
    api "${libs.slf4j}"
    compileOnly "${libs.jpa}"
    compileOnly "${libs.fastjson}"
    compileOnly "${libs["spring-tx"]}"
}
