dependencies {
    api project(':mybatisx-support')
    api project(":mybatisx-auditable-plugin")
    api project(":mybatisx-pageable-plugin")
    api project(':mybatisx-batch-plugin')
    api "${libs.inject}"
    api "${libs.cglib}"
    api "${libs.slf4j}"
    implementation "${libs.jpa}"
    implementation "${libs.fastjson}"
    implementation "${libs["spring-tx"]}"
}
